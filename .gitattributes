# Set default behaviour, in case users don't have core.autocrlf set.
* text=auto

# Explicitly declare text files we want to always be normalized and converted
# to native line endings on checkout.
*.md            text
*.gitattributes text

# Declare line breaks for file types during checkout.
*.ps1    text  eol=lf
*.psm1   text  eol=lf
*.psd1   text  eol=lf
*.psc1   text  eol=lf
*.ps1xml text  eol=lf
*.clixml text  eol=lf
*.xml    text  eol=lf
*.txt    text  eol=crlf
*.md     text  eol=crlf

# Denote all files that are truly binary and should not be mergeable.
*.dll binary
*.exe binary
